[project]
name = "one-ai-of-a-time"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "datasets>=5.0.0",
    "huggingface-hub>=1.20.1",
    "prompt-toolkit>=3.0.52",
    "python-dotenv>=1.2.2",
]
